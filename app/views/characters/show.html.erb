<div class="container">
  <h4 class="notice-heading"><%= "This is someone else's property (can't edit)..." if @character.user.id != current_user.id %></h4>
  <%= render "core_attributes", locals: {character: @character} %>
  <hr>
  <%= render "abilities", locals: {character: @character} %>
</div>

